class Wire{
    // The wire class will consist of a head and tail through which they can deliver power
    constructor{
        this.head = [];
        this.tail = [];
        this.state = 0; // Should only ever be 0 or 1
    }
    
    show(){
        
        if(this.state = 0){
            
        } else if (this.state = 1){
            
        }
        
    }
}